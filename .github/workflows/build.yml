name: Unity Windows Build

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build-windows64:
    name: Build Windows (x64)
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4ав

      # Кэш Unity Library
      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-${{ runner.os }}-${{ hashFiles('**/Packages/packages-lock.json') }}
          restore-keys: |
            Library-${{ runner.os }}-

      # ✅ Активация Unity
      - name: Activate Unity
        uses: game-ci/unity-activate@v2
        with:
          unityVersion: 2022.3.21f1
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}

      # 🏗️ Сборка Windows 64 (через стабильный контейнер)
      - name: Build Windows 64
        uses: game-ci/unity-builder@v4
        with:
          targetPlatform: StandaloneWindows64
          unityVersion: 2022.3.21f1
          dockerImage: unityci/editor:ubuntu-2022.3.21f1-windows-mono-3.1.0   # ✅ рабочий контейнер
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}

      # 📦 Загрузка результата
      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: VayneEffortsAvaLab-Windows64
          path: build/StandaloneWindows64
